---
- name: "Ensure directory {{ conf_dir }} is present"
  file:
    path: "{{ conf_dir }}"
    state: directory

- name: "Assemble configurations from {{ build_dir }} to {{ conf }}"
  assemble:
    src: "{{ build_dir }}"
    dest: "{{ conf }}"
