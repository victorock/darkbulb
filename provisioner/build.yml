- name: "Google Cloud Lab Environment"
  hosts: localhost
  gather_facts: false

  tasks:
    - name: "Create Topology in Google Cloud"
      include_role:
        name: victorock.google-cloud
        tasks_from: run
      loop: "{{ instances }}"
      loop_control:
        loop_var: gce_instance
