<domain type='kvm'>
  <name>{{ kvm_instance_name }}</name>
  <memory unit='GiB'>{{ kvm_instance_memory }}</memory>
  <vcpu>2</vcpu>

  <os>
    <type arch='i686' machine='pc'>hvm</type>
    <boot dev='hd'/>
    <bootmenu enable='yes'/>
  </os>

  <features>
    <acpi/>
    <apic/>
    <pae/>
  </features>

  <clock offset='utc'/>

  <on_poweroff>destroy</on_poweroff>
  <on_reboot>restart</on_reboot>
  <on_crash>restart</on_crash>

  <devices>
    <disk type='block' device='disk'>
      <driver name='qemu' type='qcow2' cache='writeback'/>
      <source dev='{{ kvm_img_path }}/{{ kvm_instance_name }}.qcow2'/>
      <target dev='vda' bus='virtio'/>
    </disk>

{%  for left_node, left_link, right_node, right_link in left_nodes, left_links, right_nodes, right_links %}
{%    if inventory_hostname ~ left_node %}
{%      for n in range(0,) %}
    <devices>
      <interface type='udp'>
        <source address='{{ right_node }}' port='{{ 10000+right_link }}'>
          <local address='{{ left_node }}' port='{{ 10000+left_link }}'/>
        </source>
      </interface>
    </devices>
{%      endfor %}
{%    elif inventory_hostname ~ right_node %}

    <serial type='pty'>
      <target port='0'/>
    </serial>
    <console type='pty'>
      <target type='serial' port='0'/>
    </console>

    <input type='mouse' bus='ps2'/>
    <graphics type='vnc' port='-1' autoport='yes'/>

    <video>
      <model type='cirrus' vram='9216' heads='1'/>
    </video>
  </devices>
</domain>
