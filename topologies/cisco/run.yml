#!/usr/bin/env ansible-playbook
---
- name: "Topology Runner"
  hosts: localhost
  connection: local
  gather_facts: no
  become: no

  tasks:
    - name: "Run Topology"
      include_role:
        name: vagrant-topology
        tasks_from: start
